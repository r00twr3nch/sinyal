# Client → game server URL (build-time for Vite).
# Leave empty for same-origin (local Vite proxy / Render monolith).
# For GitHub Pages / custom domain UI, set to your Render backend:
# VITE_SERVER_URL=https://sinyal-1-1h9v.onrender.com

# Server (production host sets PORT automatically)
# PORT=3001
# CLIENT_ORIGIN=https://r00twr3nch.github.io,https://yunusemredurak.com.tr,http://localhost:5173

