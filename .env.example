# Client → game server URL.
# Leave empty for same-origin (local Vite proxy / Render monolith).
# For GitHub Pages, set to your backend, e.g.:
# VITE_SERVER_URL=https://sinyal-xxxx.onrender.com

# Server (production host sets PORT automatically)
# PORT=3001
# CLIENT_ORIGIN=https://r00twr3nch.github.io,http://localhost:5173
